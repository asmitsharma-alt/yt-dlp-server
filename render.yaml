services:
  - type: web
    name: yt-dlp-server
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: "3001"
      - key: ALLOWED_ORIGIN
        value: "*"
      - key: NODE_ENV
        value: "production"
